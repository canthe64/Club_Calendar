@* Shared "contact us" footer (operator request, 2026-09-04) - one component rather than the same
   markup pasted into each of the staff pages that carry it (Calendar, Search, Settings), so a future
   wording/address change can't land on some of them and not others. The three public (anonymous,
   hand-built-HTML) pages that carry the identical line use PublicPageFooter.Html instead - this
   component's Blazor markup has no equivalent there, since those pages are plain strings, not
   components. *@
<div style="margin-top:24px;padding-top:12px;border-top:1px solid #e7ecef;font-size:12px;color:#90a0ab">
    For problems or questions with this page, contact <a href="mailto:techcommittee@curlingseattle.org" style="color:#2d5f8a">Tech Committee.</a>
</div>
